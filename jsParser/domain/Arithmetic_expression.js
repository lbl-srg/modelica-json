class Arithmetic_expression {
  constructor (arithmetic_term_list) {
    arithmetic_term_list != null ? arithmetic_term_list.length != 0 ? this.arithmetic_term_list = arithmetic_term_list : '' : ''
  }
}
exports.Arithmetic_expression = Arithmetic_expression
