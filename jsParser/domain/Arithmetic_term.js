class Arithmetic_term {
  constructor (add_op, term) {
    add_op != null ? add_op.length != 0 ? this.add_op = add_op : '' : ''
    term != null ? this.term = term : ''
  }
}
exports.Arithmetic_term = Arithmetic_term
