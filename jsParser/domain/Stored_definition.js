class Stored_definition {
  constructor (within, final_class_definitions) {
    within != null ? this.within = within : ''
    final_class_definitions != null ? this.final_class_definitions = final_class_definitions : ''
  }
}
exports.Stored_definition = Stored_definition
