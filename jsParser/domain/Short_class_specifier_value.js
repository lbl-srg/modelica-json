class Short_class_specifier_value {
  constructor (base_prefix, name, array_subscripts, class_modification, comment, enum_list) {
    base_prefix != null ? this.base_prefix = base_prefix : ''
    name != null ? this.name = name : ''
    array_subscripts != null ? this.array_subscripts = array_subscripts : ''
    class_modification != null ? this.class_modification = class_modification : ''
    comment != null ? this.comment = comment : ''
    enum_list != null ? this.enum_list = enum_list : ''
  }
}
exports.Short_class_specifier_value = Short_class_specifier_value
