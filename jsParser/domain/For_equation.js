class For_equation {
  constructor (for_indices, loop_equations) {
    for_indices != null ? this.for_indices = for_indices : ''
    loop_equations != null ? loop_equations.length != 0 ? this.loop_equations = loop_equations : '' : ''
  }
}
exports.For_equation = For_equation
