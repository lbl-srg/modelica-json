class Element_modification_or_replaceable {
  constructor (each, is_final, element_modification, element_replaceable) {
    each != null ? this.each = each : ''
    is_final != null ? this.is_final = is_final : ''
    element_modification != null ? this.element_modification = element_modification : ''
    element_replaceable != null ? this.element_replaceable = element_replaceable : ''
  }
}
exports.Element_modification_or_replaceable = Element_modification_or_replaceable
