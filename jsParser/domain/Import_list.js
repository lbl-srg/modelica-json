class Import_list {
  constructor (identifier_list) {
    identifier_list != null ? identifier_list.length != 0 ? this.identifier_list = identifier_list : '' : ''
  }
}
exports.Import_list = Import_list
