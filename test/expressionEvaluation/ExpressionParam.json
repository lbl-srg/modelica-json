{
  "a": "1",
  "b": "a*2",
  "exp": "b^b",
  "equal": "a == b",
  "diff": "a <> b",
  "leq": "a <= b",
  "geq": "a >= b",
  "lt": "a < b",
  "gt": "a > b",
  "ifexp": " if b > a then 1 else 0",
  "elseifexp": " if a > b then 0 elseif b == 2 then 1 else -1",
  "notexp": " if not (a < b) then 1 else 2",
  "andnotexp": " if a < b and not (a < b) then 1 else 2",
  "notexppar": " if not ([object Object]) then 1 else 2",
  "andnotexppar": " if ([object Object]) and not ([object Object]) then 1 else 2",
  "fil": "Array(4).fill(a)",
  "siz1": "math.size(fil)[1 - 1]",
  "siz": "math.size(fil)",
  "arr": "{{{i,j} for i in 1:a} for j in 1:b}",
  "fnabs": "math.abs(-b)",
  "fnsign": "math.sign(b)",
  "fnsqrt": "math.sqrt(b)",
  "fndiv": "math.floor(math.divide(a,b))",
  "fnmod": "math.mod(a,b)",
  "fnrem": "rem(a,b)",
  "fnceil": "math.ceil(a/b)",
  "fnfloor": "math.floor(a/b)",
  "fnint": "math.floor(a/b)",
  "fnmax": "math.max(a,b)",
  "fnmaxarr": "math.max(fil)",
  "fnmin": "math.min(a,b)",
  "fnminarr": "math.min(fil)",
  "fnsum": "math.sum(fil)"
}